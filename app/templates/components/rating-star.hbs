{{#if hasBlock}}
  {{yield stars}}
{{else}}
  {{#each this.stars as |star|}}
    <button type="button" class="rating-star">
      {{#if star.isFull}}
        {{fa-icon "star"}}
      {{else}}
        {{fa-icon "star-o"}}
      {{/if}}
    </button>
  {{/each}}
{{/if}}
